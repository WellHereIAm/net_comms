Should one more User-like struct that will be generally used, User should be stored on server only and used to create usable User-like
    that will hold less information, like no password etc, just what´s needed.
Change from usize to u32 or u64.